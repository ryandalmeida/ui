FROM ubuntu
 
USER root
 
RUN apt-get update && \
RUN apt-get install -y python openssh-client iputils-ping vim && \
RUN apt-get update && \
RUN apt-add-repository -y ppa:ansible/ansible
RUN apt-get update
RUN apt-get -y install ansible
 
WORKDIR /ansible
